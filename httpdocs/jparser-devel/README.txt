jParser development
-------------------

The jParser libraries are compiled by the PLUG framework.
The basic idea is that *many* class files can be maintained, but distributed in a more efficient format for production use.
The example PHP file in this directory shows how to import the classes at runtime for library development.
