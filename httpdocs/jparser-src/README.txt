jParser library source
----------------------

** These files will not run **
They are designed purely to build the compiled libraries found in "../jparser-libs"
- To rebuild the library files see the build script in the root of the package.
- To inspect the library source code see the class files in "../../PLUG".
- To develop the library classes, see example in "../jparser-devel"
